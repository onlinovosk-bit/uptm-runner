# STACK 03 — Plan

**Objective:** Produce a wave plan 0–7 with ownership, tests, and adversarial tests. No execution yet.

## Constraints
- Waves are sequential; no crossing.
- Patch loop max 3 per failure cluster.
- Cursor execution via contract only; Ruflo via adapter only.

## Outputs
- Plan artifact referencing `waves/*.yaml`
